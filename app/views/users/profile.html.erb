<div class="container my-2">
  <div class="col-12 col-lg-6 mx-auto">
    <div class="card">
      <div class="card-block" style="padding: 2rem;">
        <h2>Éditer mon profil</h2>
        <%= form_for(@user) do |f| %>
        <div class="field">
          <%= f.label :description %>
          <br />
          <%= f.text_area :description, rows: 8, class: 'form-control', autofocus: true, autocomplete: "description" %>
        </div>
        <br>
        <div class="actions">
          <%= f.submit "Update", class: 'btn btn-primary' %>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
